name: Build Android APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4          # ← 升级：v3 → v4
    
    - name: Set up Python
      uses: actions/setup-python@v5      # ← 建议也升级到 v5
      with:
        python-version: '3.10'
    
    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          python3-pip \
          build-essential \
          git \
          ffmpeg \
          libsdl2-dev \
          libsdl2-image-dev \
          libsdl2-mixer-dev \
          libsdl2-ttf-dev \
          libportmidi-dev \
          libswscale-dev \
          libavformat-dev \
          libavcodec-dev \
          zlib1g-dev \
          libgstreamer1.0 \
          gstreamer1.0-plugins-base \
          gstreamer1.0-plugins-good \
          libffi-dev \
          libssl-dev \
          autoconf \
          automake \
          libtool \
          pkg-config
    
    - name: Install Buildozer
      run: |
        pip install buildozer cython==0.29.36
    
    - name: Cache Android SDK
      uses: actions/cache@v4              # ← 升级：v3 → v4
      with:
        path: |
          ~/.buildozer/android/platform
          ~/.buildozer/android/packages
        key: ${{ runner.os }}-android-sdk-${{ hashFiles('buildozer.spec') }}
        restore-keys: |
          ${{ runner.os }}-android-sdk-
    
    - name: Build APK
      run: |
        cd android
        buildozer -v android debug
      env:
        BUILDOZER_WARN_ON_ROOT: '0'
    
    - name: Upload APK
      uses: actions/upload-artifact@v4    # ← 升级：v3 → v4（这是导致失败的关键）
      with:
        name: bambu-rfid-apk
        path: android/bin/*.apk
        retention-days: 30
    
    - name: Create Release
      if: github.event_name == 'push' && contains(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v2  # ← 建议升级到 v2
      with:
        files: android/bin/*.apk
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
